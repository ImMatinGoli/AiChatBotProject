import time


# بعدا که کتابخونه‌های واقعی رو نصب کردیم، این‌ها رو از کامنت در میاریم:
# import openai
# import google.generativeai as genai

def generate_response(user_message, model_name):
    """
    این تابع اصلی‌ترین تابع سرویس ماست.
    وظیفه‌ش اینه که بفهمه کاربر کدوم مدل رو انتخاب کرده
    و پیام رو بده دست مسئول همون مدل.
    """

    # برای اینکه حس واقعی بودن داشته باشه، یه تاخیر کوچیک (مثلا ۱ ثانیه) میدیم
    # انگار هوش مصنوعی داره فکر میکنه
    time.sleep(1)

    try:
        # --- مسیریابی درخواست‌ها ---

        if model_name == 'gpt-4o':
            return ask_openai_gpt4(user_message)

        elif model_name == 'gpt-3.5-turbo':
            return ask_openai_gpt35(user_message)

        elif model_name == 'gemini-pro':
            return ask_google_gemini(user_message)

        elif model_name == 'llama-2':
            return ask_local_llama(user_message)

        else:
            # اگه مدلی انتخاب شده بود که ما نمیشناختیم (مثلا هک یا باگ)
            return "مدل انتخاب شده معتبر نیست. لطفاً مجدداً تلاش کنید."

    except Exception as e:
        # اگه هر خطایی (قطعی نت، ارور API و...) پیش اومد، برنامه کرش نکنه
        return f"متاسفانه خطایی رخ داد: {str(e)}"


# ==========================================
# توابع اختصاصی برای هر مدل (فعلاً حالت تستی)
# ==========================================

def ask_openai_gpt4(message):
    # اینجا بعدا کد واقعی وصل شدن به GPT-4 رو میزنیم
    return f"✨ [پاسخ GPT-4o]:\nمن دقیق‌ترین مدل فعلی هستم. پیام شما این بود:\n«{message}»"


def ask_openai_gpt35(message):
    # اینجا کد GPT-3.5 میاد (ارزون‌تر و سریع‌تر)
    return f"⚡ [پاسخ GPT-3.5]:\nمن سریع جواب میدم. شما گفتید:\n«{message}»"


def ask_google_gemini(message):
    # اینجا کد وصل شدن به گوگل جمینای میاد
    return f"🤖 [پاسخ Gemini Pro]:\nمن مدل گوگل هستم و می‌توانم تحلیل‌های خوبی ارائه دهم. متن شما:\n«{message}»"


def ask_local_llama(message):
    # اینجا کد وصل شدن به مدل لوکال روی سیستم خودت
    return f"🦙 [پاسخ Llama 2]:\nمن روی سرور شخصی شما اجرا می‌شوم و نیازی به اینترنت ندارم. پیام:\n«{message}»"